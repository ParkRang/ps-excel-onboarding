[pytest]
# 백엔드 import는 backend/ 루트 기준 절대경로이므로 pytest 실행 시 경로를 추가한다.
# 반드시 backend/ 를 CWD로 실행할 것 (예: cd backend && pytest).
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
